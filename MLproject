name: tutorial

python_env: python_env.yaml

entry_points:
  main:
    parameters:
      sampling: {type: string, default: 'undersampling'}
    command: | 
        python main.py \
          --sampling={sampling}